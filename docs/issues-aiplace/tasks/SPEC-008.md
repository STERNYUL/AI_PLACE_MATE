# SPEC-008 · 근거 4항목 계약

> **웨이브** `P1a-contracts` · **라벨** `spec, contract, backend, evidence-service, priority:high, phase-0, blocks-all`
> **원본** [`docs/issues-aiplace/P1a-contracts.md`](../P1a-contracts.md#spec-008)
> **원장** `TASKS-ai-place-v1.0.md` (TASKS-AIPLACE-MVP-001 v1.1 · 50건) · **SRS** `[SRS]ai-place -mate-SRSv1.0.md` (SRS-AIPLACE-MVP-001 v1.9)

### 🎯 Summary
- **Task ID** `SPEC-008` · **Epic** Contract · **Must / H**
- **목적** **이 제품의 정체성이 걸린 계약.** 네 태스크가 공유하며, 정의가 갈리면 통합에서 터진다.

### 🔗 References
- `REQ-FUNC-010` 근거 제시 — "후보마다 선정 이유·근거 속성·확인 일자·확인 주체를 표기"
- §4.5.1 근거 무결성 · §4.5.2 정렬 이전 배치 · §8.3 규칙 1
- §8.2 `Verification` 상태 전이 — `VERIFIED` / `STALE` / `RECHECK_REQUIRED`
- `REQ-FUNC-011` 신선도 90일 · `REQ-NF-011` 경고 누락률 0%
- 공유 대상 `EVD-A`(FR-021) · `EVD-C`(FR-025) · `AGT-C`(FR-046) · `CLI-C`(FR-068)

### 근거 4항목 — `REQ-FUNC-010` 원문 그대로

| # | 항목 | 출처 |
| --- | --- | --- |
| 1 | **선정 이유** | `REQ-FUNC-010` |
| 2 | **근거 속성** | `REQ-FUNC-010` — `Attribute` 참조 |
| 3 | **확인 일자** | `REQ-FUNC-010` — `Verification.verified_at` |
| 4 | **확인 주체** | `REQ-FUNC-010` — `Verification` |

### ✅ Task Breakdown
- [ ] 4항목 각각의 **데이터 출처** 확정 (엔터티·필드 수준)
- [ ] **누락 판정 시점** 확정 — §4.5.2는 **정렬 이전**을 규정
- [ ] **누락 시 동작** 확정 — 제외인지 표기인지
- [ ] `Verification` 3상태와 근거 유효성의 관계 확정
- [ ] 90일 경과(`STALE`) 시 근거가 유효한가 **(미정)**
- [ ] 판정형 문구 금지 규칙 연결 (§8.3 규칙 3)

### 확정해야 할 핵심 3가지

**① 누락 판정 시점.** §4.5.2는 *"정렬 이전 단계 배치"* 를 규정한다.
즉 근거가 없는 후보는 **정렬에 참여하지 않는다.** 정렬 후 필터링이 아니다 — 그러면 Top-3가 3개 미만이 될 수 있다.

**② `STALE` 상태의 근거는 유효한가. (미정 — 확정 필요)**
`REQ-FUNC-011`은 90일 초과 시 **경고를 병기**하라고만 한다. 제외하라고 하지 않는다.
그런데 §8.1은 *"근거 없는 후보 반환 금지"* 다. **`STALE`이 "근거 없음"인지 "근거 있으나 오래됨"인지 SRS가 구분하지 않는다.**

| 해석 | 결과 |
| --- | --- |
| `STALE` = 근거 있음 (경고만) | 후보로 반환. `REQ-FUNC-011` 문언에 충실 |
| `STALE` = 근거 없음 | 후보에서 제외. `REQ-NF-011` 경고 누락률 0%에 안전하나 **Fill이 줄어든다** |

**③ `RECHECK_REQUIRED` 상태.** 불일치 신고 수신 시 전이된다(§8.2). 이 상태의 후보 취급도 같은 문제를 갖는다.

### 🧪 Acceptance Criteria

**Scenario 1 · 정상 — 4항목 완비**
- **Given** 선정 이유·근거 속성·확인 일자·확인 주체가 모두 있는 후보
- **When** 정렬 대상에 넣음
- **Then** 정상적으로 정렬에 참여하고 후보로 반환된다

**Scenario 2 · 예외 (§4.5.3) — 확인 주체 결락**
- **Given** 4항목 중 확인 주체만 없는 후보
- **When** 정렬 이전 검증을 수행함
- **Then** **정렬에 참여하지 않는다** (§4.5.2 "정렬 이전 단계")

**Scenario 3 · 경계 — `verified_at + 90일` 당일**
- **Given** 확인 일자로부터 정확히 90일이 경과한 후보
- **When** 상태를 판정함
- **Then** `STALE`로 전이되고 경고가 병기된다 (`REQ-FUNC-011`). **후보 포함 여부는 (미정)**

**Scenario 4 · 근거 무결성 — 전 소비처 일관성**
- **Given** 동일 후보에 대해 `EVD-A`(FR-021 · 검증) · `EVD-C`(FR-025 · 공유 카드) · `AGT-C`(FR-046 · 제안 정렬) · `CLI-C`(FR-068 · 카드 렌더)이 각각 판정함
- **When** 네 곳의 판정 결과를 대조함
- **Then** **네 곳이 동일한 결론을 낸다.** 이 시나리오가 이 계약의 존재 이유다

### ⚙️ Technical & Non-Functional Constraints
- **§4.5.2** 근거 검증은 **정렬 이전**에 배치
- **§8.1** 근거 없는 후보 반환 **금지**
- **`REQ-NF-011`** 신선도 경고 누락률 **0%**
- **§8.3 규칙 3** 판정형 문구 금지 — 근거 문장은 **확인된 사실만** 진술
- 이 계약은 **4개 태스크가 공유**한다. 재정의 금지

### 🏁 DoD
공통 DoD 5개. 추가로 —
- [ ] **`STALE` 후보의 반환 여부가 확정되었는가?**
- [ ] `RECHECK_REQUIRED` 후보의 취급이 확정되었는가?
- [ ] 4항목의 데이터 출처가 엔터티·필드 수준으로 지정되었는가?
- [ ] 네 소비처(`EVD-A`(FR-021)·`EVD-C`(FR-025)·`AGT-C`(FR-046)·`CLI-C`(FR-068)) 담당자가 동일 해석에 동의했는가?

### 🚧 Dependencies & Blockers
- **Depends on** `SPEC-001` (#94)
- **Blocks** `SPEC-002` (#95) · `SPEC-004` (#97) · `EVD-A` (#120)(FR-021) · `EVD-C` (#122)(FR-025) · `AGT-C` (#151)(FR-046) · `CLI-C` (#137)(FR-068) · `MOCK-003` (#110) · `MOCK-004` (#111)
- **미정 — 확정 필요**
  - **`STALE` 근거의 유효성** — PM + 개발팀 리드 (평가서 §4 항목, 최우선)
  - `RECHECK_REQUIRED` 후보 취급
  - Top-3 3개 고정과의 충돌 (`SPEC-002` Scenario 4와 동일 사안)

### 공통 DoD — 웨이브 `P1a-contracts` 전체

- [ ] 계약이 `docs/api-aiplace.yaml`(OpenAPI)에 반영되었는가?
- [ ] 소비 측(구현·Mock·클라이언트) 담당자가 검토하고 동의했는가?
- [ ] **(미정)** 항목이 전부 해소되었거나 확정 담당자·기한이 지정되었는가?
- [ ] 계약 변경 절차(승인자·통보 대상)가 정해졌는가?
- [ ] SRS §8.1.1의 수치를 **반올림·요약 없이** 그대로 옮겼는가?

