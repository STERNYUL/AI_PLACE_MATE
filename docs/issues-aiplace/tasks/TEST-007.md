# TEST-007 · 신선도 경고 및 판정형 문구 검증

> **웨이브** `P3b-tests-evidence` · **라벨** `test, verification, evidence, priority:critical, phase-1, identity`
> **원본** [`docs/issues-aiplace/P3b-tests-evidence.md`](../P3b-tests-evidence.md#test-007)
> **원장** `TASKS-ai-place-v1.0.md` (TASKS-AIPLACE-MVP-001 v1.1 · 50건) · **SRS** `[SRS]ai-place -mate-SRSv1.0.md` (SRS-AIPLACE-MVP-001 v1.9)

**대상** `REQ-FUNC-011` · **TC** `TC-FUNC-011`

### 🎯 Summary
- **Task ID** `TEST-007` · **Must / H**
- **목적** **`REQ-NF-011` 경고 누락률 0%** 와 **§8.3 규칙 3 판정형 0건** — 두 불변 규칙을 동시에 잰다.

### 🔗 References
- `REQ-FUNC-011`(90일 신선도) · **`REQ-NF-011`**(누락률 **0%**) · **§8.3 규칙 3**(판정 금지) · §1.1
- §5.1 `TC-FUNC-011` · 대응 구현 `EVD-B`(생성·차단) `CLI-C`(렌더) **`UX-C`(라이팅 가이드)**

### ⚠️ 판정형 사전이 곧 테스트 규격이다

`P2b` 미정 1번과 `P2f`의 `UX-015`가 만나는 지점이다.

**무엇이 판정형인지 규칙이 없으면 이 테스트를 쓸 수 없다.**
`UX-015` 라이팅 가이드가 정본이 되고, 이 태스크가 그것을 **실행 가능한 검증**으로 만든다.

```
UX-015 라이팅 가이드 (판정형 어휘 기준)
        ↓ 정본
TEST-007 판정형 검출 스캐너
        ↓ 검증 대상
EVD-B 서버 생성 문장 · CLI-C 화면 카피 · MCH-A 매장 입력
```

**세 곳을 같은 스캐너로 검사해야** 기준이 하나임이 보장된다.

### ✅ Task Breakdown
- [ ] `TC-FUNC-011` 실행 케이스 전환
- [ ] **신선도 경고 누락 검출** — `FRESH`·`STALE`·`RECHECK_REQUIRED` 3상태 (`MOCK-004`)
- [ ] **판정형 문구 스캐너** — `UX-015` 가이드 기반
- [ ] 스캐너를 **세 경로에 적용** — `EVD-B` · `CLI-C` · `MCH-A`
- [ ] 90일 경계 케이스
- [ ] 경고 **시각 위계** 검증 방법 **(미정 — 자동화 가능한가)**

### 🧪 검증 시나리오

**정상** — `FRESH` 후보에 경고가 붙지 않고, `STALE` 후보에 붙는다
**예외 (§4.5.3)** — 추론 서비스 장애 시 대체 문안도 **판정형 검사를 통과**한다
**경계** — `verified_at + 90일` **당일** 판정
**불변 규칙** — **경고 누락 0건 · 판정형 0건.** 두 수치 모두 0이 아니면 실패

### ⚙️ Constraints
- **`REQ-NF-011`** 누락률 **0%** — 화면 기준으로 판정
- **§8.3 규칙 3** 판정 금지 — 제품 정체성
- 시각 위계(경고가 묻히지 않는가)는 **자동 검증이 어렵다** — 수기 검토 주기 명시 필요

### 🏁 DoD
공통 5개. 추가로 —
- [ ] **경고 누락이 0건인가?**
- [ ] **판정형이 0건인가?** (서버·화면·매장 입력 3경로)
- [ ] 스캐너가 `UX-015` 가이드를 정본으로 참조하는가?
- [ ] 시각 위계 검증 방법이 정해졌는가?

### 🚧 Dependencies & Blockers
- **Depends on** `EVD-B` (#121) `CLI-C` (#137) **`UX-C` (#142)(UX-015)**
- **Blocks** Phase 1 게이트 신뢰성
- **미정 — 확정 필요** **판정형 어휘 기준** (`UX-015` · `P2b` 1번) · 시각 위계 검증 자동화 여부

### 공통 DoD — 웨이브 `P3b-tests-evidence` 전체

- [ ] SRS §5.1의 해당 TC ID를 **실행 가능한 케이스로 전환**했는가?
- [ ] **CI에서 자동 실행**되는가?
- [ ] 실패 시 어느 요구사항이 깨졌는지 리포트에 나오는가?
- [ ] 픽스처가 버전 관리되는가?
- [ ] 발견한 공백이 **구현 이슈 DoD로 역류**되었는가?

