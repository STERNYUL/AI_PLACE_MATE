# SPEC-002 · `POST /v1/query` 계약

> **웨이브** `P1a-contracts` · **라벨** `spec, contract, backend, search-service, priority:high, phase-0, critical-path`
> **원본** [`docs/issues-aiplace/P1a-contracts.md`](../P1a-contracts.md#spec-002)
> **원장** `TASKS-ai-place-v1.0.md` (TASKS-AIPLACE-MVP-001 v1.1 · 50건) · **SRS** `SRS-ai-place-v1.0.md` (SRS-AIPLACE-MVP-001 v1.9)

### 🎯 Summary
- **Task ID** `SPEC-002` · **Epic** Contract · **Must / H**
- **목적** 제품의 핵심 호출. **Top-3 카드에 무엇이 실려 오는가**를 확정한다. 클라이언트(`CLI-C` · FR-068)의 유일한 착수 기준점이다.

### 🔗 References
- SRS §8.1.1 `POST /v1/query` 행 · §8.1 "p95 ≤ 1,000ms, 후보 정확히 3개, 근거 없는 후보 반환 금지"
- `REQ-FUNC-002`(인당가 범위) · `REQ-FUNC-008`(필수 입력 0개) · `REQ-FUNC-009`(폴백) · `REQ-FUNC-010`(근거) · `REQ-FUNC-014`(Top-3 고정)
- `REQ-NF-001` 응답 시간 · §4.5.2 정렬 · §8.3 규칙 2
- 공유 계약 `SPEC-001` · **`SPEC-008`(근거 4항목)**

### ✅ Task Breakdown
- [ ] 요청 DTO — **조건 0개 허용**(§8.1.1 "조건 0개 허용(필수 입력 없음)")
- [ ] 응답 DTO — 후보 **정확히 3개** 배열
- [ ] 후보 항목 필드 확정 (아래)
- [ ] 폴백 전환 신호 필드 확정 (`REQ-FUNC-009`)
- [ ] '예산 초과 N곳' 요약 필드 (`REQ-FUNC-003`)
- [ ] 유사 메뉴 대체 사실 명시 필드 (`REQ-FUNC-007`, `REQ-NF-010`)
- [ ] 페이지네이션 부재 명시 (§8.3 규칙 2)

### 응답 항목 — §8.1.1 "포함 항목"에서 그대로

| 필드 | SRS 원문 | 비고 |
| --- | --- | --- |
| 후보 id | "후보 id" | `POST /v1/share-cards`의 입력이 된다 (§8.1.1 상호 관계) |
| 인당가 범위 | "인당가 범위" | `PriceProfile` 하한/평균/상한 · 단위 **원** |
| 대표 메뉴 | "대표 메뉴" | — |
| 상황 속성 | "상황 속성" | — |
| 근거 문장 | "근거 문장" | `SPEC-008` 참조 |
| 확인 일자·주체 | "확인 일자·주체" | `Verification` — `SPEC-008` 참조 |

**여섯 필드가 SRS 원문 그대로다.** 여기에 임의로 추가하지 않는다.

### 🧪 Acceptance Criteria

**Scenario 1 · 정상 — 조건 있는 질의**
- **Given** 자연어 조건 1줄과 지역·인원이 주어짐
- **When** `POST /v1/query`를 호출함
- **Then** `200`과 함께 후보가 **정확히 3개** 반환되고, 각 후보에 여섯 필드가 모두 있으며, **p95 ≤ 1,000ms**다

**Scenario 2 · 예외 (§4.5.3) — 파싱 실패 후 폴백**
- **Given** 자연어 파서가 조건을 해석하지 못함
- **When** 동일 호출을 함
- **Then** 오류가 아니라 **폴백 전환 신호**와 구조화 필터 후보군이 반환되고, 클라이언트가 전환 고지를 표시한다 (`REQ-FUNC-009`)

**Scenario 3 · 경계 — 조건 0개**
- **Given** 조건·지역·인원이 **전부 비어 있는** 요청 (§8.1.1 "조건 0개 허용")
- **When** 호출함
- **Then** `400`이 아니라 `200`과 후보 3개가 반환된다 (`REQ-FUNC-008` 필수 입력 필드 0개)

**Scenario 4 · 근거 무결성 — 근거 없는 후보 반환 금지**
- **Given** 근거 4항목 중 하나가 결락된 후보가 정렬 대상에 있음
- **When** 호출함
- **Then** **그 후보는 응답에서 제외**된다. 3개를 채우지 못하면 **(미정 — 확정 필요)**
  - §8.1은 "후보 정확히 3개"와 "근거 없는 후보 반환 금지"를 **동시에** 요구한다. 두 규칙이 충돌하는 구간의 동작이 SRS에 없다

### ⚙️ Technical & Non-Functional Constraints
- **§8.1** p95 **≤ 1,000ms** · 후보 **정확히 3개** · 근거 없는 후보 반환 **금지**
- **§8.1.1** 단위 — 금액 **원**, 거리 **m**, 지연 **ms** · 동기 호출
- **§8.3 규칙 2** 페이지네이션 없음
- `REQ-NF-001` 측정 조건 **(미정 — 확정 필요)** — 데이터량·RPS·캐시 상태가 SRS에 없다

### 🏁 DoD
공통 DoD 5개. 추가로 —
- [ ] Scenario 4의 충돌(3개 고정 ↔ 근거 없는 후보 제외)이 확정되었는가?
- [ ] 여섯 필드가 §8.1.1 원문과 **문자 단위로** 일치하는가?

### 🚧 Dependencies & Blockers
- **Depends on** `SPEC-001` · `SPEC-008`
- **Blocks** `SRC-D`(FR-019 · 구현) · `CLI-C`(FR-068 · 클라이언트) · `MOCK-002` · `MOCK-003` · `MOCK-004`
- **미정 — 확정 필요**
  - **"3개 고정" ↔ "근거 없는 후보 제외" 충돌 시 동작** — PM + 개발팀 리드
  - `REQ-NF-001` 측정 조건 — PM + 시스템 운영자 (평가서 §4 항목)

### 공통 DoD — 웨이브 `P1a-contracts` 전체

- [ ] 계약이 `docs/api-aiplace.yaml`(OpenAPI)에 반영되었는가?
- [ ] 소비 측(구현·Mock·클라이언트) 담당자가 검토하고 동의했는가?
- [ ] **(미정)** 항목이 전부 해소되었거나 확정 담당자·기한이 지정되었는가?
- [ ] 계약 변경 절차(승인자·통보 대상)가 정해졌는가?
- [ ] SRS §8.1.1의 수치를 **반올림·요약 없이** 그대로 옮겼는가?

