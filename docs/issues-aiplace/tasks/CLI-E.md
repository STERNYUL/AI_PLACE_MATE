# CLI-E · 성능 최적화 · RUM · 이벤트 계측

> **웨이브** `P2e-client` · **라벨** `feature, frontend, client, performance, tracking, priority:high, phase-1`
> **원본** [`docs/issues-aiplace/P2e-client.md`](../P2e-client.md#cli-e)
> **원장** `TASKS-ai-place-v1.0.md` (TASKS-AIPLACE-MVP-001 v1.1 · 50건) · **SRS** `SRS-ai-place-v1.0.md` (SRS-AIPLACE-MVP-001 v1.9)

**원문** `FR-077` `FR-078` `FR-079`

### 🎯 Summary
- **Task ID** `CLI-E` · **Must / M (M×3)**
- **목적** **KPI가 실제로 측정되게 만든다.** 이벤트 22종이 클라이언트에서 발생하지 않으면 `TRK-C`가 산출할 것이 없다.

### 🔗 References
- **`REQ-NF-006`**(LCP ≤ 2.5s · **4G 실측**) · §6.3 · **§6.1.1**(이벤트 22종)
- 선행 `CLI-C` `TRK-B` `UX-A` · 계약 `SPEC-009`(이벤트 스키마)

### ✅ Task Breakdown
- [ ] **`FR-077`** 초기 렌더 **LCP ≤ 2.5s** 최적화
- [ ] **`FR-078`** **RUM 계측** — **4G 실측 조건**
- [ ] **`FR-079`** 클라이언트 이벤트 계측 **22종**
- [ ] 22종 중 클라이언트 발생분 식별 — 서버 발생분과 구분 **(미정)**
- [ ] 이벤트 전송 실패 시 재시도·버퍼링
- [ ] 개인정보 미포함 검증 (`PRV-A` FR-062 연동)

### ⚠️ 4G 실측이 요구의 핵심이다

`REQ-NF-006`은 LCP 2.5초를 **4G 실측 조건**으로 규정한다.
개발 장비의 wifi에서 측정한 값은 이 요구를 충족한 증거가 되지 않는다.

**측정 방법이 요구의 일부다** — 어떤 네트워크 프로파일로, 어떤 단말에서, 몇 회 측정해 p95를 내는지.
`IN-C` 관측 체계와 같은 자리에서 정해야 한다.

**그리고 인앱 브라우저에서 측정해야 한다** (`CLI-A` R6) — 일반 모바일 브라우저 값과 다를 수 있다.

### 🧪 Acceptance Criteria

**Scenario 1 · 정상 — LCP 달성**
- **Given** 4G 프로파일 · 지도 앱 내 탭
- **When** 초기 화면을 로드함
- **Then** **LCP p95 ≤ 2.5s** (`REQ-NF-006`)

**Scenario 2 · 예외 (§4.5.3) — 이벤트 전송 실패**
- **Given** 네트워크가 끊김
- **When** 이벤트가 발생함
- **Then** **버퍼링 후 재전송**된다. 유실되면 KPI 누락률이 올라가 `TRK-D` 미공표로 이어진다

**Scenario 3 · 경계 — 22종 전수 발생**
- **Given** 전체 사용자 여정을 완주함
- **When** 발생 이벤트를 수집함
- **Then** **클라이언트 발생분이 빠짐없이 발생**한다. 어느 이벤트가 클라이언트 책임인지가 확정돼 있어야 한다

**Scenario 4 · 근거 무결성 — 개인정보 미포함**
- **Given** 전송되는 모든 이벤트
- **When** 페이로드를 검사함
- **Then** 취향·비고·출발지 원문이 **평문으로 실리지 않는다** (`REQ-NF-015` · `REQ-NF-013`)

### ⚙️ Technical & Non-Functional Constraints
- **`REQ-NF-006`** LCP **≤ 2.5s** · **4G 실측**
- **§6.1.1** 이벤트 22종 · 공통 속성 4개
- **인앱 브라우저 환경에서 측정** — R6
- 이벤트 유실이 **KPI 미공표**로 직결 (§6.1.3 5% 규칙)

### 🏁 DoD
공통 DoD 5개. 추가로 —
- [ ] **4G 실측 방법이 문서화되고 재현 가능한가?**
- [ ] 클라이언트 이벤트 22종 분담이 확정되고 전수 발생하는가?
- [ ] 이벤트 유실률이 측정되고 5% 미만인가?
- [ ] 페이로드에 개인정보가 없는가?

### 🚧 Dependencies & Blockers
- **Depends on** `CLI-C` `TRK-B` `UX-A` `SPEC-009`
- **Blocks** `TRK-C` KPI 산출의 실효성
- **미정 — 확정 필요** **4G 실측 방법·프로파일** · 22종 중 클라이언트 발생분 분담

### 공통 DoD — 웨이브 `P2e-client` 전체

- [ ] **네이버 지도 앱 내 탭에서** 동작을 검증했는가? (ADR-006 · R6)
- [ ] `MOCK-`만으로 화면이 완주되는가? (백엔드 미완 상태 검증)
- [ ] **4G 실측 조건**에서 `REQ-NF-006`을 만족하는가?
- [ ] 근거 없는 정보가 화면에 렌더되지 않는가? (§8.3 규칙 1)
- [ ] 판정형 문구가 화면에 없는가? (§8.3 규칙 3 · `UX-C` 라이팅 가이드)

