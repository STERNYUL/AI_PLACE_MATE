# SRC-D · Top-3 정렬 및 `POST /v1/query`

> **웨이브** `P2a-search` · **라벨** `feature, backend, search-service, priority:high, phase-1, critical-path`
> **원본** [`docs/issues-aiplace/P2a-search.md`](../P2a-search.md#src-d)
> **원장** `TASKS-ai-place-v1.0.md` (TASKS-AIPLACE-MVP-001 v1.1 · 50건) · **SRS** `SRS-ai-place-v1.0.md` (SRS-AIPLACE-MVP-001 v1.9)

**원문** `FR-018` `FR-019`

### 🎯 Summary
- **Task ID** `SRC-D` · **Must / H (H×1+M×1)**
- **목적** **제품의 얼굴.** 모든 검색 경로가 여기로 모이고 클라이언트가 이것만 호출한다.

### 🔗 References
- 계약 **`SPEC-002`** · **`SPEC-008`**
- `REQ-FUNC-014`(Top-3 고정) · `REQ-NF-001`(p95) · §4.5.2(정렬) · §8.1 · §8.1.1 · §8.3 규칙 2
- 선행 `SRC-B` `SRC-C` **`EVD-A`(근거 검증 — 정렬 이전)**

### ✅ Task Breakdown
- [ ] **`FR-018`** 적합도 정렬 + **Top-3 고정** (페이지네이션 없음)
- [ ] **`FR-019`** `POST /v1/query` 구현 — `SPEC-002` 계약 준수
- [ ] **근거 검증을 정렬 이전에 배치** (§4.5.2 · `EVD-A`)
- [ ] 폴백 전환 신호 전달 (`SRC-A`)
- [ ] '예산 초과 N곳' 요약 전달 (`SRC-B`)
- [ ] 대체 사실 전달 (`SRC-C`)
- [ ] **3개 미충족 시 동작** 구현 — `SPEC-002` 확정 결과

### ⚠️ "3개 고정"과 "근거 없는 후보 제외"가 충돌한다

`SPEC-002` Scenario 4가 지적한 항목이다. §8.1이 두 규칙을 **동시에** 요구한다.

```
후보 정확히 3개              ← REQ-FUNC-014 · §8.1
근거 없는 후보 반환 금지      ← §8.1
```

**근거 결락으로 정렬 대상이 2개만 남으면 무엇을 반환하는가.** SRS에 답이 없다.
`SPEC-008`의 `STALE` 유효성 판정과 **같은 뿌리**이며, 함께 결정해야 한다.

### 🧪 Acceptance Criteria

**Scenario 1 · 정상 — Top-3 반환**
- **Given** 조건과 근거 4항목 완비 후보 다수
- **When** `POST /v1/query`를 호출함
- **Then** `200`과 후보 **정확히 3개**, 각 후보에 §8.1.1의 여섯 필드가 있고 **p95 ≤ 1,000ms**다

**Scenario 2 · 예외 (§4.5.3) — 파싱 실패 폴백**
- **Given** `SRC-A`가 폴백 전환 신호를 반환함
- **When** 질의를 처리함
- **Then** 구조화 필터 후보군으로 Top-3를 구성하고 **전환 사실을 응답에 실어** 클라이언트가 고지하게 한다

**Scenario 3 · 경계 — 정렬 동점**
- **Given** 적합도가 동일한 후보가 4개 이상
- **When** Top-3를 구성함
- **Then** **(미정 — 확정 필요)** 동점 규칙이 SRS에 없다. 규칙이 없으면 **먼저 색인된 후보가 항상 이긴다**

**Scenario 4 · 근거 무결성 — 정렬 이전 배치**
- **Given** 근거 4항목 중 하나가 결락된 후보
- **When** 정렬을 수행함
- **Then** **그 후보는 정렬에 참여하지 않는다** (§4.5.2). 정렬 후 필터가 아니다 — 그러면 3개를 못 채운다

### ⚙️ Technical & Non-Functional Constraints
- **§8.1** p95 **≤ 1,000ms** · 후보 **정확히 3개** · 근거 없는 후보 반환 **금지**
- **§8.3 규칙 2** 페이지네이션 없음
- **§4.5.2** 근거 검증은 **정렬 이전**
- **`REQ-NF-001` 측정 조건 미정** — 데이터량·RPS·캐시 상태 (평가서 §4)
- **광고 상품 없음** — 노출 순서를 판매하지 않는다 (§1.1 핵심 원칙)

### 🏁 DoD
공통 DoD 5개. 추가로 —
- [ ] **"3개 고정 ↔ 근거 제외" 충돌이 확정되고 구현되었는가?**
- [ ] 근거 검증이 **정렬 이전**에 배치되었는가?
- [ ] 정렬 동점 규칙이 확정되었는가?
- [ ] 여섯 필드가 §8.1.1 원문과 일치하는가?

### 🚧 Dependencies & Blockers
- **Depends on** `SPEC-002` (#95) `SPEC-008` (#101) `SRC-B` (#117) `SRC-C` (#118) `EVD-A` (#120)
- **Blocks** `CLI-C` (#137) `AGT-C` (#151)(0건 회귀) `TRK-C` (#126)
- **미정 — 확정 필요**
  - **3개 고정 ↔ 근거 없는 후보 제외 충돌** (PM + 개발팀 리드 · `SPEC-008`과 함께)
  - **정렬 동점 규칙** — 신규 발견
  - `REQ-NF-001` 측정 조건

### 공통 DoD — 웨이브 `P2a-search` 전체

- [ ] `SPEC-002` 계약과 응답이 **스키마 검증**으로 일치하는가?
- [ ] `SPEC-008` 근거 4항목 정의를 **재정의하지 않고 참조만** 했는가?
- [ ] SRS 수치를 반올림·요약 없이 옮겼는가?
- [ ] 상태 비저장인가? (`IN-D` 수평 확장 전제)
- [ ] `IN-C` 관측성에 계측 지점이 연결되었는가?

