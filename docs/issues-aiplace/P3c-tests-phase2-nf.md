# P3c · 검증 태스크 (Phase 2 · 비기능) — `TEST-011` ~ `TEST-014d`

**웨이브:** 제작 순서 11번 · **Phase 1 / 2**
**근거:** `SRS-ai-place-v1.0.md` v1.9 **§5.1** · 정본 Step 3
**선행 문서** [`P3a-tests-search.md`](P3a-tests-search.md) · [`P3b-tests-evidence.md`](P3b-tests-evidence.md)

> **`TEST-011`~`TEST-013`은 Phase 2 조건부다** — [`P2g-phase2.md`](P2g-phase2.md)의 8건과 함께 이월된다.
> **`TEST-014`는 비기능 요구 32건 전부를 담당하는 단일 태스크였다.** 이 비대칭이 이 문서의 핵심 논점이었고,
> **`TEST-014a`~`014d` 4분할로 해소했다.** 성능·가용성 10 · 데이터/결과 품질 4 · 보안/개인정보 10 · 비용/KPI 8 = 32건 전수.

## 수록 이슈 7건

| 이슈 | 대상 요구 | TC ID | 대응 구현 | Phase |
| --- | --- | --- | --- | --- |
| `TEST-011` | 콘솔·근거 없는 문구 차단 `019`~`021` `027` | TC-FUNC-019~021·027 | `MCH-A` `CLI-G` `UX-H` | **2** |
| `TEST-012` | 소환·180초 마감·0건 회귀 `022` `023` `025` | TC-FUNC-022·023·025 | `AGT-A` `AGT-B` `AGT-C` | **2** |
| `TEST-013` | 제안 정렬·불이행 `024` `026` | TC-FUNC-024·026 | `AGT-C` `CLI-F` | **2** |
| `TEST-014a` | **성능·가용성** `REQ-NF-001`~`008` `012` `027` (10) | TC-NF 동 번호 | `IN-C~E` `IN-G` `SRC-D` `CLI-E` | 1 |
| `TEST-014b` | **데이터·결과 품질** `009` `010` `011` `024` (4) | TC-NF 동 번호 | `IDX-C` `SRC-A` `SRC-C` `EVD-A` `EVD-B` | 1 |
| `TEST-014c` | **보안·개인정보** `013`~`018` `025` `026` `031` `032` (10) | TC-NF 동 번호 | `IN-B` `PRV-A` `PRV-B` `RSV-C` | **1말** |
| `TEST-014d` | **비용·KPI** `019`~`023` `028`~`030` (8) | TC-NF 동 번호 | `TRK-C~E` `IN-F` | 1 |

## 공통 DoD

- [ ] SRS §5.1의 해당 TC ID를 **실행 가능한 케이스로 전환**했는가?
- [ ] CI에서 자동 실행되는가?
- [ ] 실패 시 어느 요구사항이 깨졌는지 리포트에 나오는가?
- [ ] 발견한 공백이 **구현 이슈 DoD로 역류**되었는가?

---
---

## TEST-011 · 콘솔 및 근거 없는 문구 차단 검증 {#test-011}

**labels** `test, verification, merchant, priority:high, phase-2, conditional`
**대상** `REQ-FUNC-019`~`021` `027` · **TC** `TC-FUNC-019~021·027`

### 🎯 Summary
- **Task ID** `TEST-011` · **Should / M** · **Phase 2 조건부**
- **목적** **매장 입력 경로에서도 근거 무결성이 지켜지는지** 검증한다.

### 🔗 References
- `REQ-FUNC-019`(3화면·5항목) · `REQ-FUNC-020`(수용 조건 매칭) · `REQ-FUNC-021`(**조건 밖 소환 배제**) · `REQ-FUNC-027`(**1회 클릭**)
- **§8.3 규칙 7**(근거 없는 문구 저장 차단) · §5.1 `TC-FUNC-019~021·027`
- 대응 구현 `MCH-A` `CLI-G` `UX-H` · **`TEST-007` 스캐너 재사용**

### ⚠️ `TEST-007`의 판정형 스캐너를 여기에 재사용한다

`P3b` 역류 5번이 지적한 항목이다.

```
UX-015 라이팅 가이드
   └─→ TEST-007 판정형 스캐너
          ├─ EVD-B  서버 생성 문장
          ├─ CLI-C  화면 카피
          └─ MCH-A  매장 콘솔 입력  ← 이 태스크
```

**세 경로에 같은 스캐너를 쓰지 않으면 매장 입력이 우회 경로가 된다.**
§8.3 규칙 7이 매장 입력을 명시적으로 다루는 이유다.

### ✅ Task Breakdown
- [ ] `TC-FUNC-019~021·027` 실행 케이스 전환
- [ ] 설정 **3화면 · 필수 5항목** 완주 검증
- [ ] **1회 클릭 수정** 실측
- [ ] **판정형 입력 차단** — `TEST-007` 스캐너 재사용
- [ ] **조건 밖 소환 배제** 검증
- [ ] 확인 주체·일자 자동 기록 검증
- [ ] 콘솔 **2FA** 검증 (`IN-B` FR-041)

### 🧪 검증 시나리오

**정상** — 매장이 3화면으로 5항목을 등록하고 확인 정보가 자동 기록된다
**예외 (§4.5.3)** — 판정형 문구 입력 시 **저장이 차단**된다 (§8.3 규칙 7)
**경계** — 수용 조건 상한과 **정확히 같은** 요청의 매칭 판정 **(미정)**
**불변 규칙** — **조건 밖 매장이 소환되지 않는다.** 위반 0건

### ⚙️ Constraints
- **§8.3 규칙 7** — 근거 무결성이 **입력 경로에도** 적용
- **`REQ-FUNC-027`** 1회 클릭 — 신선도 유지의 실질 수단
- **`SPEC-010` 미신설** — 계약 없이 케이스를 쓰면 구현에 종속된다

### 🏁 DoD
공통 4개. 추가로 —
- [ ] 판정형 차단이 **`TEST-007`과 같은 스캐너**인가?
- [ ] 조건 밖 소환 배제 위반이 0건인가?
- [ ] 1회 클릭이 실측되는가?
- [ ] 2FA가 검증되는가?

### 🚧 Dependencies & Blockers
- **Depends on** `MCH-A` `CLI-G` `UX-H` `IN-B` · **`SPEC-010`**
- **미정 — 확정 필요** `SPEC-010` 신설 · 수용 조건 경계값 판정

---

## TEST-012 · 소환·180초 마감·0건 회귀 검증 {#test-012}

**labels** `test, verification, agent-room, priority:high, phase-2, conditional`
**대상** `REQ-FUNC-022` `023` `025` · **TC** `TC-FUNC-022·023·025`

### 🎯 Summary
- **Task ID** `TEST-012` · **Should / M** · **Phase 2 조건부**
- **목적** **시간과 공정성**을 검증한다. 180초가 서버 시각인지, 소환이 재현 가능한지.

### 🔗 References
- `REQ-FUNC-022`(소환 3~5곳 · 0곳 즉시 미개시) · `REQ-FUNC-023`(180초) · **`REQ-FUNC-025`**(0건 Top-3 회귀)
- **§8.3 규칙 13**(서버 시각) · §8.3 규칙 5 · §5.1 `TC-FUNC-022·023·025`
- 대응 구현 `AGT-A` `AGT-B` `AGT-C` `CLI-F` `UX-G`

### ⚠️ 소환 선정의 재현성이 §1.1 원칙의 검증 항목이다

`P2g` 미정 2번이다. **3~5곳 선정 규칙이 없으면 이 태스크는 공정성을 검증할 수 없다.**

*"노출 순서를 판매하지 않는다"* 는 선언은 **같은 조건에서 같은 결과가 나올 때만** 검증 가능하다.
재현되지 않으면 판매하지 않았다는 것도 증명할 수 없다.

`SRC-D`의 정렬 동점 규칙, `AGT-C`의 가중치 규칙과 **같은 성격의 검증 항목**이다.

### ✅ Task Breakdown
- [ ] `TC-FUNC-022·023·025` 실행 케이스 전환
- [ ] **소환 3~5곳 재현성** 검증 — 동일 조건 반복
- [ ] **0곳 시 즉시 미개시** 검증 — 방이 생성되지 않는다
- [ ] **180초 서버 시각** 판정 검증 — 클라이언트 시각 조작 시도
- [ ] **제안 0건 → Top-3 회귀** 검증 (`REQ-FUNC-025`)
- [ ] 채널 끊김·재연결 시 제안 복구 검증
- [ ] 3곳 미만 시 개시 여부 **(미정)**

### 🧪 검증 시나리오

**정상** — 조건 만족 매장 5곳이 소환되고 방이 개시된다
**예외 (§4.5.3)** — 제안 0건 시 **Top-3로 회귀**한다 (`REQ-FUNC-025`)
**경계** — **클라이언트 시각을 앞당긴 상태**에서 마감 판정 — 서버 시각을 따른다
**불변 규칙** — **동일 조건에서 소환 대상이 재현된다.** 재현 불가면 공정성 미검증

### ⚙️ Constraints
- **§8.3 규칙 13** 서버 시각 — **조작 시나리오를 포함**해야 의미가 있다
- **§1.1** 노출 순서 비판매 — 재현성이 그 증명 수단
- 인앱 브라우저 채널 유지 — `CLI-A` 실측 결과 반영

### 🏁 DoD
공통 4개. 추가로 —
- [ ] **소환 재현성이 검증되는가?**
- [ ] 클라이언트 시각 조작이 마감을 바꾸지 못하는가?
- [ ] 0건 회귀가 `SRC-D` 결과를 재사용하는가?
- [ ] 0곳 시 방이 생성되지 않는가?

### 🚧 Dependencies & Blockers
- **Depends on** `AGT-A` `AGT-B` `AGT-C` `CLI-F`
- **미정 — 확정 필요** **소환 선정 규칙** (`P2g` 2번 · 재현성 검증의 전제) · 3곳 미만 개시 여부 · 채널 방식

---

## TEST-013 · 제안 정렬·불이행 검증 {#test-013}

**labels** `test, verification, agent-room, priority:high, phase-2, conditional`
**대상** `REQ-FUNC-024` `026` · **TC** `TC-FUNC-024·026`

### 🎯 Summary
- **Task ID** `TEST-013` · **Should / M** · **Phase 2 조건부**
- **목적** **적합도 정렬이 재현 가능한지**와 **불이행 제재에 근거가 있는지** 검증한다.

### 🔗 References
- `REQ-FUNC-024`(**적합도 우선 정렬**) · `REQ-FUNC-026`(불이행 기록 · **가중치 하향**)
- §4.5.1 · §1.1 · §5.1 `TC-FUNC-024·026` · 대응 구현 `AGT-C` `CLI-F`

### ⚠️ 산출식과 하향 규칙이 없으면 검증 대상이 없다

`P2g` 미정 3·4번이다.

| 미정 | 이 태스크에 미치는 영향 |
| --- | --- |
| **적합도 산출식** | 정렬 결과를 무엇과 대조하는지 모른다 |
| **가중치 하향 폭·회복** | 하향이 맞게 됐는지 판정할 기준이 없다 |

**`TEST-013`은 두 항목 확정 전에는 착수 불가**이며, `TEST-010`(노쇼 정산)과 같은 구조의 차단이다.

### ✅ Task Breakdown
- [ ] `TC-FUNC-024·026` 실행 케이스 전환 — **규칙 확정 후**
- [ ] **적합도 정렬 재현성** 검증
- [ ] 정렬 동점 처리 검증 **(`SRC-D`와 같은 공백)**
- [ ] **불이행 기록의 근거 추적** 검증
- [ ] 가중치 하향 폭·회복 검증
- [ ] 제안에 대한 **근거 4항목 검증** (`EVD-A` 재사용 · `TEST-006` 대조 대상 확장)

### 🧪 검증 시나리오

**정상** — 제안이 적합도 순으로 정렬된다
**예외 (§4.5.3)** — 근거 없는 제안이 **등록 거부**된다 (`EVD-A` 동일 판정)
**경계** — 적합도 **동점** 시 순서 — **(미정 · `SRC-D`와 동일 성격)**
**불변 규칙** — **모든 가중치 하향에 추적 가능한 근거가 있다.** 근거 없는 제재는 §1.1 위반

### ⚙️ Constraints
- **§4.5.1** 근거 무결성 — 제안·제재 양쪽에 적용
- **§1.1** 노출 순서 비판매 — 정렬 재현성이 증명 수단
- `TEST-006`의 네 소비처 대조에 **`AGT-C`가 다섯 번째로 추가**된다

### 🏁 DoD
공통 4개. 추가로 —
- [ ] **적합도 산출식과 하향 규칙이 확정되어 케이스에 반영되었는가?**
- [ ] 정렬이 재현 가능한가?
- [ ] 모든 하향에 근거가 추적되는가?
- [ ] 근거 없는 제안이 거부되는가?

### 🚧 Dependencies & Blockers
- **Depends on** `AGT-C` `CLI-F` `EVD-A`
- **미정 — 확정 필요** **적합도 산출식** · **가중치 하향 폭·회복 규칙** (전부 `P2g` · **착수 조건**) · 동점 처리

---

## TEST-014a · 성능·가용성 검증 {#test-014a}

**labels** `test, verification, non-functional, performance, priority:critical, phase-1`
**대상** `REQ-NF-001`~`008` · `012` · `027` (10건) · **TC** 동 번호
**분할 출처** `TEST-014`(비기능 32건 단일) 4분할 중 1

### 🎯 Summary
- **Task ID** `TEST-014a` · **Must / H**
- **목적** **응답 시간·처리량·가용성·복구 목표**를 실측으로 판정한다. 부하 도구가 필요한 항목만 모았다.

### 🔗 References
- `REQ-NF-001` p95 1,000ms · p99 2,000ms · `002` 메뉴 질의 400ms(캐시 히트 120ms) · `003` 공유 카드 3,000ms
- `REQ-NF-004` 첫 제안 p50 60s · `005` 3,000 RPS · `006` LCP 4G 2.5s
- `REQ-NF-007` 가용성 99.5%(월 다운타임 ≤ 3h39m) · `008` 5xx 0.3%·결제 0.1%
- `REQ-NF-012` RTO 30분 · `027` RPO 5분
- 대응 구현 `IN-C`(SLO 계측) `IN-D`(수평 확장) `IN-E`(이중화·백업) `IN-G`(부하 환경) `SRC-D` `CLI-E`

### ✅ Task Breakdown
- [ ] `IN-G` 부하 환경에서 `REQ-NF-001`~`003` p95·p99 측정
- [ ] **3,000 RPS 하에서 001~003 목표 유지** 확인 (`REQ-NF-005` — 적용 시점 확정 후)
- [ ] LCP **4G 실측** (`REQ-NF-006` — 실측 방법 미정)
- [ ] 가용성·오류율 집계 검증 — **결제 API 분리 계측** (`REQ-NF-008`)
- [ ] 복구 훈련으로 RTO 30분 · RPO 5분 판정 (`REQ-NF-012` `027`)
- [ ] 10건 커버리지 대조표

### 🧪 검증 시나리오

**정상** — 10건 전수가 실행되고 각각 통과·실패가 판정된다
**예외 (§4.5.3)** — 측정 조건 미확정 항목은 **판정 불가로 표시**된다. 통과로 처리하지 않는다
**경계** — 임계값 **정확히 같을 때** — **(미정 · 전 항목 공통 · `P3a` 7번)**
**불변 규칙** — **10건 중 미검증 항목이 0건이다.** 검증하지 못한 것은 실패로 센다

### ⚙️ Constraints
- `REQ-NF-004`(첫 제안 도착)는 **대화방이 있어야 측정된다.** Phase 2 게이트 미통과 시 **판정 불가로 표시**하고 통과로 세지 않는다
- `REQ-NF-005` 3,000 RPS 적용 시점 미정 (`IN-D` `IN-G`)
- `REQ-NF-006` **4G 실측 방법 미정** (`CLI-E`)

### 🏁 DoD
공통 4개. 추가로 —
- [ ] 10건 커버리지 대조표에 누락이 0건인가?
- [ ] 미검증 항목이 **통과로 처리되지 않는가?**
- [ ] 부하 시나리오가 `IN-G` 산출물과 같은 것인가?

### 🚧 Dependencies & Blockers
- **Depends on** `IN-C` `IN-D` `IN-E` `IN-G` `SRC-D` `CLI-E`
- **Blocks** Phase 1 게이트 신뢰성 (성능 축)
- **미정 — 확정 필요** 4G 실측 방법 · 3,000 RPS 적용 시점 · 임계값 경계 규약

---

## TEST-014b · 데이터·결과 품질 검증 {#test-014b}

**labels** `test, verification, non-functional, reliability, priority:critical, phase-1`
**대상** `REQ-NF-009` `010` `011` `024` (4건) · **TC** 동 번호
**분할 출처** `TEST-014`(비기능 32건 단일) 4분할 중 2

### 🎯 Summary
- **Task ID** `TEST-014b` · **Must / M**
- **목적** **파싱 실패율·빈 결과율·신선도·스키마 확장성**을 판정한다. 기능 검증(`TEST-005`·`007`)과 대상이 겹치되 **비율 지표**를 본다.

### 🔗 References
- `REQ-NF-009` 파싱 실패율 ≤ 1.5% + **폴백 전환으로 빈 화면 없음**
- `REQ-NF-010` 빈 결과 반환률 ≤ 2% + 유사 메뉴 대체로 회피
- `REQ-NF-011` **90일 초과 시 경고 표기 필수** + 주간 추적
- `REQ-NF-024` `Attribute.scope` 확장 시 회귀 없음 (ADR-001)
- 대응 구현 `IDX-A`~`C` `SRC-A` `SRC-C` `EVD-A` `EVD-B`

### ⚠️ `TEST-005`·`TEST-007`과의 경계

`TEST-005`는 **파싱 실패 시 폴백이 동작하는가**(단건 동작)를 본다.
`TEST-014b`는 **실패율이 1.5% 이하인가**(집계 비율)를 본다. 같은 코드를 다른 축으로 잰다.

`TEST-007`은 **90일 초과 후보에 경고가 붙는가**를, `TEST-014b`는 **90일 초과 속성 비율의 주간 추적이 되는가**를 본다.

**둘을 합치지 않는 이유** — 단건 판정은 Phase 1 초반에 가능하나 **비율 판정은 질의 로그가 쌓여야** 한다.

### ✅ Task Breakdown
- [ ] 파싱 실패율 **5분 윈도 집계** 검증 (`REQ-NF-009`)
- [ ] 실패 시 **빈 화면 미반환** 확인 — §8.3 규칙 5
- [ ] 빈 결과 반환률 시간당 집계 검증 (`REQ-NF-010`)
- [ ] 신선도 90일 초과 비율 **주간 추적** 검증 (`REQ-NF-011`)
- [ ] 신규 속성 추가 회귀 테스트 (`REQ-NF-024`)
- [ ] 4건 커버리지 대조표

### 🧪 검증 시나리오

**정상** — 4건 전수가 실행되고 각각 통과·실패가 판정된다
**예외 (§4.5.3)** — 로그 표본이 부족한 구간은 **판정 불가로 표시**된다
**경계** — 실패율 **정확히 1.5%** · 빈 결과 **정확히 2%** · 신선도 **정확히 90일** — **(미정 · `P3a` 7번)**
**불변 규칙** — **4건 중 미검증 항목이 0건이다**

### ⚙️ Constraints
- 비율 지표라 **질의 로그가 쌓여야 판정 가능** — Phase 1 초반에는 표본 부족
- `REQ-NF-011`의 90일은 캐시 6h와 충돌 구간이 있다 (`P1a` 미정 항목)

### 🏁 DoD
공통 4개. 추가로 —
- [ ] 4건 커버리지 대조표에 누락이 0건인가?
- [ ] `TEST-005`·`TEST-007`과 **중복 실행이 아닌 다른 축**임이 문서로 구분되는가?

### 🚧 Dependencies & Blockers
- **Depends on** `IDX-C` `SRC-A` `SRC-C` `EVD-A` `EVD-B` `TRK-B`
- **Blocks** Phase 1 게이트 신뢰성 (품질 축)
- **미정 — 확정 필요** 임계값 경계 규약 (`P3a` 7번) · 캐시 6h ↔ 신선도 90일 충돌

---

## TEST-014c · 보안·개인정보 검증 {#test-014c}

**labels** `test, verification, non-functional, security, privacy, priority:critical, phase-1-late`
**대상** `REQ-NF-013`~`018` · `025` `026` `031` `032` (10건) · **TC** 동 번호
**분할 출처** `TEST-014`(비기능 32건 단일) 4분할 중 3

### 🎯 Summary
- **Task ID** `TEST-014c` · **Must / H**
- **목적** **개인정보 처리와 보안 통제**를 감사한다. 통과 못 하면 출시할 수 없는 항목만 모았다.

### 🔗 References
- **개인정보** `REQ-NF-013` 출발지 30일 파기 · `031` 목적 제한 · `014` 개인 제약 단말 저장 · `032` 옵트인 · `015` 취향 미수집
- **보안** `REQ-NF-016` 카드 비보관(PCI-DSS 위탁) · `017` AES-256 · `018` 콘솔 2FA · `025` 감사 로그 · `026` TLS 1.3
- §8.6.4 개인정보 처리 · §8.6.5 보존 기간
- 대응 구현 `IN-B`(보안 공통) `PRV-A`(서버 측) `PRV-B`(단말 저장·옵트인) `RSV-C`(PG)

### ⚠️ Phase 1 말인 이유

선행 `PRV-A` `PRV-B` `RSV-C`가 전부 **Phase 1 말**이다.
분할 전 `TEST-014`는 `phase-1`이면서 이 셋을 선행으로 가져 **Phase 1 안에서 판정을 끝낼 수 없는 구조**였다.
분할하며 이 묶음만 `phase-1-late`로 옮겨 해소했다.

### ✅ Task Breakdown
- [ ] 출발지 정보 **30일 내 파기** 감사 (`REQ-NF-013`)
- [ ] **목적 외 접근 로그 점검** (`REQ-NF-031`)
- [ ] 개인 제약 정보 **단말 저장 경로** 검증 (`REQ-NF-014`)
- [ ] **미동의 전송 차단** 테스트 (`REQ-NF-032`)
- [ ] 취향 필드 **스키마 부재** 검증 + 수집 경로 감사 (`REQ-NF-015`)
- [ ] 카드 정보 저장 여부 감사 (`REQ-NF-016`) · AES-256 설정 감사 (`017`)
- [ ] 콘솔 **2FA 강제** + 비인가 접근 차단 (`REQ-NF-018`)
- [ ] 감사 로그 **완전성 스캔** (`REQ-NF-025`) · 전송 프로토콜 스캔 (`026`)
- [ ] 10건 커버리지 대조표

### 🧪 검증 시나리오

**정상** — 10건 전수가 실행되고 각각 통과·실패가 판정된다
**예외 (§4.5.3)** — 법무 확정 대기 항목은 **판정 불가로 표시**된다. 통과로 처리하지 않는다
**경계** — 파기 **정확히 30일 시점** — **(미정 · `P3a` 7번)**
**불변 규칙** — **10건 중 미검증 항목이 0건이다.** 이 묶음은 미검증이 곧 출시 불가다

### ⚙️ Constraints
- **백업본의 파기 처리 미정** — RPO 5분(`REQ-NF-027`) 체계와 30일 파기가 충돌한다 (`P4a` 발견 · 법무)
- **동의 이력 저장 위치 미정** — 서버에 남기면 `REQ-NF-014` 단말 저장 원칙과 충돌한다 (법무)
- **§8.6.5 보존 기간 상세 미정** — 확정 전에는 파기 시점을 잴 기준이 없다

### 🏁 DoD
공통 4개. 추가로 —
- [ ] 10건 커버리지 대조표에 누락이 0건인가?
- [ ] **법령 미정 3건**이 판정 불가로 표시되고 통과로 세지지 않는가?
- [ ] 감사 증적이 재현 가능한 형태로 남는가?

### 🚧 Dependencies & Blockers
- **Depends on** `IN-B` `PRV-A` `PRV-B` `RSV-C`
- **Blocks** 출시 가부 판정
- **미정 — 확정 필요** 백업 ↔ 파기 충돌 · 동의 이력 위치 · §8.6.5 보존 기간 — **3건 모두 법무**

---

## TEST-014d · 비용·KPI 검증 {#test-014d}

**labels** `test, verification, non-functional, tracking, priority:high, phase-1`
**대상** `REQ-NF-019`~`023` · `028`~`030` (8건) · **TC** 동 번호
**분할 출처** `TEST-014`(비기능 32건 단일) 4분할 중 4

### 🎯 Summary
- **Task ID** `TEST-014d` · **Should / M**
- **목적** **단위 경제와 사용성 KPI**를 집계 대조로 판정한다. 코드가 아니라 **집계 결과**를 검증한다.

### 🔗 References
- **비용** `REQ-NF-019` 세션당 추론 12원 · `020` 캐시 히트율 70% · `021` 단위 경제 3배 · `022` 심사 FTE 150곳당 1 · `023` 외부 단가 수수료 15% 이하
- **사용성** `REQ-NF-028` 60초·외부 채널 1개 · `029` 결정 시간 20% 단축 · `030` 불일치 신고율 10% 이하
- §6.1.2 파생 지표(`top3_render` → `candidate_select`) · §6.1.4 경쟁 대안 벤치마크
- 대응 구현 `TRK-C`(KPI 배치) `TRK-D`(결측 처리) `TRK-E`(비용·단위 경제) `IN-F`(캐시)

### ⚠️ 기준선 없이는 판정할 수 없는 항목이 있다

`REQ-NF-029`(결정 시간 **20% 단축**)는 **비교 대상 기준선**이 있어야 성립한다.
`REQ-NF-028`(60초·외부 채널 1개)도 §6.1.4 벤치마크 실측이 전제다.

**`TRK-C`의 기준선 15건 실측이 Phase 0 종료 시점에 나온다.** 그 전에는 이 두 건이 판정 불가다.

### ✅ Task Breakdown
- [ ] 세션당 추론 비용 **토큰 사용량 일간 집계** 대조 (`REQ-NF-019`)
- [ ] 캐시 히트/미스 집계로 70% 판정 (`REQ-NF-020`)
- [ ] 성사 건당 수수료 ÷ 처리 비용 **3배** 산출 (`REQ-NF-021`)
- [ ] 가맹점 수 대 심사 FTE 월간 집계 (`REQ-NF-022`)
- [ ] 탐색 60초·외부 채널 1개 벤치마크 (`REQ-NF-028`)
- [ ] 결정 시간 **20% 단축** — 기준선 대비 (`REQ-NF-029`)
- [ ] 불일치 신고율 월간 집계 10% 이하 (`REQ-NF-030`)
- [ ] 8건 커버리지 대조표

### 🧪 검증 시나리오

**정상** — 8건 전수가 실행되고 각각 통과·실패가 판정된다
**예외 (§4.5.3)** — **기준선 미실측 항목은 판정 불가로 표시**된다. 통과로 처리하지 않는다
**경계** — 히트율 **정확히 70%** · 신고율 **정확히 10%** · 배수 **정확히 3배** — **(미정 · `P3a` 7번)**
**불변 규칙** — **8건 중 미검증 항목이 0건이다**

### ⚙️ Constraints
- `REQ-NF-023`(외부 데이터 단가)은 **제휴 도입 시에만** 적용된다. 미도입이면 **해당 없음**으로 표시하고 통과로 세지 않는다
- `REQ-NF-022` 심사 FTE는 `FR-082`의 Phase 모순(온보딩 P1 ↔ 프로필 등록 P2)에 걸려 있다 (`TRK-E`)
- 기준선 15건 미실측 (`TRK-C`) — `REQ-NF-028` `029`의 전제

### 🏁 DoD
공통 4개. 추가로 —
- [ ] 8건 커버리지 대조표에 누락이 0건인가?
- [ ] **기준선 미실측 항목이 통과로 처리되지 않는가?**
- [ ] `REQ-NF-023`의 **해당 없음**이 미검증과 구분되어 기록되는가?

### 🚧 Dependencies & Blockers
- **Depends on** `TRK-C` `TRK-D` `TRK-E` `IN-F`
- **Blocks** 단위 경제 판정 · Phase 1 게이트의 비용 축
- **미정 — 확정 필요** 기준선 15건 (`TRK-C`) · `FR-082` Phase 모순 · 임계값 경계 규약

---

## 검증 웨이브 전체 요약 (`TEST-001`~`014d`)

### 착수를 막는 미정 항목 — **7건**

| # | 항목 | 막힌 검증 | 담당 |
| --- | --- | --- | --- |
| 1 | 정규화 평가셋 부재 | `TEST-001` | PM + 데이터 |
| 2 | 예산 판정 기준값 | `TEST-002` | PM |
| 3 | `STALE` 후보 유효성 | `TEST-006` | 개발팀 리드 |
| 4 | 판정형 어휘 기준 | `TEST-007` `TEST-011` | PM + 디자인 |
| 5 | 방문 확인·노쇼·정산 규칙 | `TEST-010` | PM + 사업팀 |
| 6 | 적합도 산출식 · 가중치 하향 규칙 | `TEST-013` | PM |
| 7 | 소환 선정 규칙 | `TEST-012` | PM |

### 이 웨이브가 드러낸 구조적 사실

**검증 태스크는 판정 규칙 없이 착수할 수 없다.**
구현은 규칙이 모호해도 "일단 이렇게" 만들 수 있지만, 테스트는 **무엇이 통과인지** 알아야 첫 줄을 쓴다.

원장 v1.1에 검증 태스크가 0건이었던 것이 **미정 항목이 이렇게 오래 남아 있던 이유**다.
검증을 태스크로 세우자마자 7건이 차단 항목으로 드러났다.

### 새로 제안된 사항 — **2건**

| 제안 | 내용 | 영향 |
| --- | --- | --- |
| **`TEST-014` 4분할** | 성능·가용성 · 데이터/결과 품질 · 보안/개인정보 · 비용/KPI | **적용 완료** · 총 81 → **84건** |
| **임계값 경계 일괄 규약** | 모든 임계값의 이상/초과를 한 번에 확정 | 미정 다수 일괄 해소 |

**두 번째 제안이 비용 대비 효과가 크다.** 3% · 5% · 90일 · 2시간 · 예산 기준값 · 수용 조건 상한 등
**개별로 흩어진 경계 미정이 최소 7건**이다. 하나씩 물으면 7번의 확정 회의가 필요하지만,
**"모든 임계값은 이하/이상을 포함한다"** 는 규약 하나로 정리된다.
